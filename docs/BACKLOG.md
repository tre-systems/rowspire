# Backlog

Only work not represented by the current architecture belongs here.

## Product and AI

- Expose tested search-depth presets for user-selectable difficulty.
- Decide whether ML remains a peer strategy or becomes explicitly experimental.
- Measure model quality against a fixed evaluation corpus before replacing weights.

## Quality

- Add visual-regression baselines only when the UI stabilizes enough to avoid noisy snapshots.
- Prefer generated badges or summaries over hand-maintained coverage numbers.
- Revisit bundle budgets when measured product requirements add a justified dependency.
